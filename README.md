# School-Database-Management-System
School Database Management System built with Spring Boot. Supports CRUD operations for managing students, teachers, and classes via RESTful APIs. Utilizes Hibernate/JPA for data persistence and @RestControllerAdvice for global exception handling. Uses H2 database for testing. Run with postman application.
